[project]
name = "airflow-batch-processor"
version = "0.1.0"
description = "Airflow component for batch processing"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["airflow_batch_processor"]
