#!/bin/bash
# Wrapper for Qwen Code CLI
# Standard path is preferred, fallback to local pnpm path if needed
if [ -x /usr/local/bin/qwen ]; then
    exec /usr/local/bin/qwen "$@"
else
    export PATH="$HOME/.local/share/pnpm:$PATH"
    exec "$HOME/.local/share/pnpm/qwen" "$@"
fi
