- name: create_artifact
  title: null
  description: Create a new artifact following project standards with auto-validation
  inputSchema:
    type: object
    properties:
      artifact_type:
        type: string
        enum:
        - assessment
        - audit
        - bug_report
        - design_document
        - implementation_plan
        - vlm_report
        - walkthrough
        description: Type of artifact to create (dynamically loaded from plugins)
      name:
        type: string
        description: Artifact name in kebab-case (e.g., 'api-refactor')
      title:
        type: string
        description: Human-readable title for the artifact
      description:
        type: string
        description: Optional description of the artifact
      tags:
        type: string
        description: Comma-separated tags (e.g., 'optimization,performance')
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - artifact_type
    - name
    - title
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: AgentQMS.mcp_server
    function: call_tool
- name: validate_artifact
  title: null
  description: Validate artifact(s) against naming and structure standards
  inputSchema:
    type: object
    properties:
      file_path:
        type: string
        description: Path to specific artifact to validate (optional)
      validate_all:
        type: boolean
        description: Validate all artifacts instead of single file
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: AgentQMS.mcp_server
    function: call_tool
- name: list_artifact_templates
  title: null
  description: List all available artifact templates with details
  inputSchema:
    type: object
    properties:
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: AgentQMS.mcp_server
    function: call_tool
- name: check_compliance
  title: null
  description: Check overall artifact compliance status and generate report
  inputSchema:
    type: object
    properties:
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: AgentQMS.mcp_server
    function: call_tool
- name: get_standard
  title: null
  description: Retrieve project standard or rule content by name (fuzzy match)
  inputSchema:
    type: object
    properties:
      name:
        type: string
        description: Name of the standard (e.g. 'naming-conventions', 'artifact-types')
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - name
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: AgentQMS.mcp_server
    function: call_tool
- name: get_server_info
  title: null
  description: Get information about the Project Compass MCP server
  inputSchema:
    type: object
    properties:
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: project_compass.mcp_server
    function: call_tool
- name: manage_session
  title: null
  description: Manage project sessions (export, import, list, new) to save/restore
    context.
  inputSchema:
    type: object
    properties:
      action:
        type: string
        enum:
        - export
        - import
        - list
        - new
        description: Action to perform on the session.
      session_name:
        type: string
        description: Name of the session to import (required for import action).
      note:
        type: string
        description: Note to attach to the session (optional for export).
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - action
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: project_compass.mcp_server
    function: call_tool
- name: init_experiment
  title: null
  description: Initialize a new experiment with standardized directory structure
  inputSchema:
    type: object
    properties:
      name:
        type: string
        description: Unique experiment identifier (slug format, e.g., 'baseline-kie-v2')
      description:
        type: string
        description: Optional experiment description
      tags:
        type: string
        description: Comma-separated tags (e.g., 'optimization,vlm')
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - name
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: experiment_manager.mcp_server
    function: call_tool
- name: get_experiment_status
  title: null
  description: Get detailed status of current or specific experiment
  inputSchema:
    type: object
    properties:
      experiment_id:
        type: string
        description: Experiment ID (optional, uses current if not specified)
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: experiment_manager.mcp_server
    function: call_tool
- name: add_task
  title: null
  description: Add a task to the active experiment plan
  inputSchema:
    type: object
    properties:
      description:
        type: string
        description: Task description
      experiment_id:
        type: string
        description: Experiment ID (optional, uses current if not specified)
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - description
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: experiment_manager.mcp_server
    function: call_tool
- name: log_insight
  title: null
  description: Log a key finding, decision, or failure to the experiment
  inputSchema:
    type: object
    properties:
      insight:
        type: string
        description: The insight, decision, or failure to log
      type:
        type: string
        enum:
        - insight
        - decision
        - failure
        description: Type of log entry
      experiment_id:
        type: string
        description: Experiment ID (optional, uses current if not specified)
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - insight
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: experiment_manager.mcp_server
    function: call_tool
- name: sync_experiment
  title: null
  description: Sync experiment artifacts and metadata to database
  inputSchema:
    type: object
    properties:
      experiment_id:
        type: string
        description: Experiment ID (optional, uses current if not specified)
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: experiment_manager.mcp_server
    function: call_tool
- name: adt_meta_query
  title: null
  description: 'Unified analysis tool for code understanding. Routes to specialized
    analyzers based on ''kind'' parameter. Kinds: config_access, merge_order, hydra_usage,
    component_instantiations, config_flow, dependency_graph, imports, complexity,
    context_tree, symbol_search'
  inputSchema:
    type: object
    properties:
      kind:
        type: string
        enum:
        - config_access
        - merge_order
        - hydra_usage
        - component_instantiations
        - config_flow
        - dependency_graph
        - imports
        - complexity
        - context_tree
        - symbol_search
        description: Type of analysis to perform
      target:
        type: string
        description: Target path or search query
      options:
        type: object
        description: Kind-specific options (output, component, depth, threshold, fuzzy,
          etc.)
        additionalProperties: true
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - kind
    - target
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: adt_meta_edit
  title: null
  description: 'Unified edit tool for code modification. Routes to specialized editors
    based on ''kind'' parameter. Kinds: apply_diff (fuzzy unified diff), smart_edit
    (search/replace), read_slice (line range), format (code formatting)'
  inputSchema:
    type: object
    properties:
      kind:
        type: string
        enum:
        - apply_diff
        - smart_edit
        - read_slice
        - format
        description: Type of edit to perform
      target:
        type: string
        description: Target file path or diff content
      options:
        type: object
        description: Kind-specific options (diff, search, replace, mode, strategy,
          dry_run, etc.)
        additionalProperties: true
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - kind
    - target
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: analyze_config_access
  title: null
  description: Analyze Python code for configuration access patterns (cfg.X, self.cfg.X,
    config['key']). Essential for understanding how configuration flows through the
    codebase.
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to Python file or directory to analyze (relative to project
          root or absolute)
      component:
        type: string
        description: 'Optional: Filter results by component name (e.g., ''decoder'',
          ''encoder'')'
      output:
        type: string
        enum:
        - json
        - markdown
        default: json
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: trace_merge_order
  title: null
  description: Trace OmegaConf.merge() operations and their precedence order. Critical
    for debugging configuration override issues where later merges overwrite earlier
    values.
  inputSchema:
    type: object
    properties:
      file:
        type: string
        description: Path to Python file to analyze
      explain:
        type: boolean
        default: true
        description: Include detailed precedence explanation
      output:
        type: string
        enum:
        - json
        - markdown
        default: markdown
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - file
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: find_hydra_usage
  title: null
  description: Find Hydra framework usage patterns including @hydra.main decorators,
    hydra.utils.instantiate() calls, and _target_ config patterns.
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to Python file or directory to analyze
      output:
        type: string
        enum:
        - json
        - markdown
        default: json
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: find_component_instantiations
  title: null
  description: 'Track component instantiation patterns: get_*_by_cfg() factory calls,
    registry.create() patterns, and direct class instantiation. Helps trace where
    components are created and what config sources them.'
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to Python file or directory to analyze
      component:
        type: string
        description: 'Optional: Filter by component type (e.g., ''decoder'', ''encoder'',
          ''head'')'
      output:
        type: string
        enum:
        - json
        - markdown
        default: json
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: explain_config_flow
  title: null
  description: 'Generate a high-level summary of configuration flow through a file:
    imports, entry points, merge operations, and instantiations. Provides a bird''s-eye
    view for understanding complex config handling.'
  inputSchema:
    type: object
    properties:
      file:
        type: string
        description: Path to Python file to analyze
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - file
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: analyze_dependency_graph
  title: null
  description: Build module dependency graph showing imports, class inheritance, and
    call relationships. Detects circular dependencies. Outputs JSON, markdown, or
    Mermaid diagram.
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to Python file or directory to analyze
      include_stdlib:
        type: boolean
        default: false
        description: Include standard library imports in graph
      output:
        type: string
        enum:
        - json
        - markdown
        - mermaid
        default: json
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: analyze_imports
  title: null
  description: Categorize imports as stdlib, third-party, or local. Detects potentially
    unused imports. Useful for dependency auditing and dead code detection.
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to Python file or directory to analyze
      show_unused:
        type: boolean
        default: true
        description: Include potentially unused imports in output
      output:
        type: string
        enum:
        - json
        - markdown
        default: json
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: analyze_complexity
  title: null
  description: 'Calculate code complexity metrics: cyclomatic complexity, nesting
    depth, LOC, parameter count. Identifies functions exceeding thresholds for refactoring
    candidates.'
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to Python file or directory to analyze
      threshold:
        type: integer
        default: 10
        description: Complexity threshold for warnings
      output:
        type: string
        enum:
        - json
        - markdown
        default: json
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: context_tree
  title: null
  description: Generate annotated directory tree with semantic context. Extracts module
    docstrings, __all__ exports, and key class/function definitions for rich AI navigation.
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Directory path to analyze
      depth:
        type: integer
        default: 3
        description: Maximum directory depth to traverse
      output:
        type: string
        enum:
        - json
        - markdown
        default: markdown
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: intelligent_search
  title: null
  description: Search for symbols by name or qualified path with fuzzy matching. Resolves
    Hydra _target_ paths, finds class definitions, and suggests corrections for typos.
  inputSchema:
    type: object
    properties:
      query:
        type: string
        description: Symbol name or qualified path (e.g., 'TimmBackbone' or 'ocr.core.models.encoder.TimmBackbone')
      root:
        type: string
        description: 'Root directory to search (default: project root)'
      fuzzy:
        type: boolean
        default: true
        description: Enable fuzzy matching for typos
      threshold:
        type: number
        default: 0.6
        description: Minimum similarity for fuzzy matches (0.0-1.0)
      output:
        type: string
        enum:
        - json
        - markdown
        default: markdown
        description: Output format
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - query
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: apply_unified_diff
  title: null
  description: Apply a unified diff to files with fuzzy matching. Handles whitespace
    drift and minor code changes. Returns detailed report of applied/failed hunks.
  inputSchema:
    type: object
    properties:
      diff:
        type: string
        description: Unified diff text (git diff format)
      strategy:
        type: string
        enum:
        - exact
        - whitespace_insensitive
        - fuzzy
        default: fuzzy
        description: Matching strategy for finding content to replace
      dry_run:
        type: boolean
        default: false
        description: If true, show what would change without modifying files
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - diff
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: smart_edit
  title: null
  description: Intelligent search/replace in a file. Supports exact, regex, and fuzzy
    matching modes with fallback.
  inputSchema:
    type: object
    properties:
      file:
        type: string
        description: Path to file to edit
      search:
        type: string
        description: Text or pattern to search for
      replace:
        type: string
        description: Replacement text
      mode:
        type: string
        enum:
        - exact
        - regex
        - fuzzy
        default: exact
        description: Matching mode
      all_occurrences:
        type: boolean
        default: false
        description: 'Replace all matches (default: first only)'
      dry_run:
        type: boolean
        default: false
        description: Preview changes without modifying
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - file
    - search
    - replace
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: read_file_slice
  title: null
  description: Read a specific line range from a file. Use this for targeted editing
    of large files instead of reading the entire file.
  inputSchema:
    type: object
    properties:
      file:
        type: string
        description: Path to file
      start_line:
        type: integer
        description: Starting line number (1-indexed)
      end_line:
        type: integer
        description: Ending line number (1-indexed, inclusive)
      context_lines:
        type: integer
        default: 0
        description: Additional lines to include before/after range
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - file
    - start_line
    - end_line
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
- name: format_code
  title: null
  description: Format code using black, ruff, or isort. Normalizes code style after
    edits.
  inputSchema:
    type: object
    properties:
      path:
        type: string
        description: Path to file or directory to format
      style:
        type: string
        enum:
        - black
        - ruff
        - isort
        default: black
        description: Formatter to use
      check_only:
        type: boolean
        default: false
        description: Only check, don't modify
      force:
        type: boolean
        description: Force execution, bypassing some compliance checks (use with caution)
        default: false
    required:
    - path
  outputSchema: null
  icons: null
  annotations: null
  meta: null
  execution: null
  implementation:
    module: agent_debug_toolkit.mcp_server
    function: call_tool
