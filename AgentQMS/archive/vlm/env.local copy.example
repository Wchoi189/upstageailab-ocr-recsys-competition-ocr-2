# Local overrides example. Copy to `.env.local` for machine-specific secrets.
# Values here will override those provided in `.env`.

# Example overriding backend preference
VLM_BACKEND=solar_pro2

# Example overriding CLI command path
QWEN_VLM_COMMAND=/usr/local/bin/qwen-vl
