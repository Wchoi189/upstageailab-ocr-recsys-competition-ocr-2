# Local overrides example. Copy to `.env.local` for machine-specific secrets.
# Values here will override those provided in `.env`.

# Backend preference for local runs (use CLI Qwen by default)
VLM_BACKEND=dashscope

# CLI command path (adjust if your qwen-vl binary lives elsewhere)
QWEN_VLM_COMMAND=qwen
