# @package _global_
defaults:
  - /data/runtime/performance/balanced@runtime
  - override /domain: recognition
  - override /hardware: rtx3060
  - _self_

# Experiment-specific logic overrides
trainer:
  max_epochs: 50
  precision: "16-mixed"

data:
  batch_size: 64
