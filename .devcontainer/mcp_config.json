{
    "mcpServers": {
        "unified_project": {
            "command": "uv",
            "args": [
                "run",
                "--directory",
                "${workspaceFolder}",
                "python",
                "scripts/mcp/unified_server.py"
            ],
            "env": {
                "PROJECT_ROOT": "${workspaceFolder}"
            }
        }
    }
}
