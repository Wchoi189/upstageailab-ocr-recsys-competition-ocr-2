# MCP Visibility Extension

VS Code extension for real-time visibility into MCP tool calls.

## Features

- 📊 Real-time dashboard with tool call statistics
- ⚠️ Policy violation alerts
- 📋 Recent call log with filtering
- 🎨 VS Code theme integration

## Development

```bash
cd mcp-visibility-extension
npm install
npm run compile
```

Press `F5` to open Extension Development Host.

## Usage

1. Open Command Palette (`Cmd/Ctrl+Shift+P`)
2. Run `MCP Visibility: Show Dashboard`
3. Or click the pulse icon in the Activity Bar
