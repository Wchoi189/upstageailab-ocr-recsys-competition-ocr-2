---
# OCR Inference Console - AI Documentation Index
# Entry point for AI agents working with OCR console
# Audience: AI agents only | Format: machine-parseable YAML

meta:
  app_name: "OCR Inference Console"
  version: "1.0.0"
  status: "production"
  last_updated: "2025-12-21"
  tech_stack: "FastAPI + React + TypeScript + Vite"

quickstart:
  file: "quickstart.yaml"
  purpose: "Startup commands, ports, health checks"

architecture:
  backend_services: "architecture/backend-services.yaml"
  frontend_context: "architecture/frontend-context.yaml"
  error_handling: "architecture/error-handling.yaml"

contracts:
  api_endpoints: "contracts/api-endpoints.yaml"
  pydantic_models: "contracts/pydantic-models.yaml"
  typescript_types: "contracts/typescript-types.yaml"

workflows:
  add_feature: "workflows/add-feature.yaml"
  debug_errors: "workflows/debug-errors.yaml"

# Critical paths (always accurate)
paths:
  backend_entry: "apps/ocr-inference-console/backend/main.py"
  frontend_entry: "apps/ocr-inference-console/src/main.tsx"
  services: "apps/ocr-inference-console/backend/services/"
  components: "apps/ocr-inference-console/src/components/"
  context: "apps/ocr-inference-console/src/contexts/"

# Ports (DO NOT ASSUME - always check)
ports:
  backend: 8002
  frontend: 5173
