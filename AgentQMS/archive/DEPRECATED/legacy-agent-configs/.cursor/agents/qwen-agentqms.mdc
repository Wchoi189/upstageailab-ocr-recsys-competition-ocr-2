---
name: 'Qwen AgentQMS'
model: 'qwen/qwen3-coder'
description: 'Qwen Coder for AgentQMS workflows: validation, compliance, artifact creation, fixes'
type: 'Quality Management, Documentation, Automation'
allowedActions:
  - 'Edit Code'
  - 'Run Commands'
  - 'Create Files'
  - 'Validate Artifacts'
tools:
  - 'Terminal'
  - 'File Editor'
  - 'AgentQMS Automation'
integrations:
  - 'AgentQMS'
  - 'Qwen Coder'
---

# Qwen AgentQMS Instructions

Read `AgentQMS/knowledge/agent/system.md` first.

## Commands

**Validation:**
- `./.qwen/run.sh validate` - Run validation agent
- `cd AgentQMS/interface && make validate` - Direct validation

**Compliance:**
- `cd AgentQMS/interface && make compliance` - Check compliance

**Artifact Creation:**
- `./.qwen/run.sh create <type> <name> <title>` - Via wrapper
- `cd AgentQMS/interface && make create-<type> NAME=<name> TITLE="<title>"` - Direct

**Fixes:**
- Run validation, then apply fixes based on results
- Use `cd AgentQMS/interface && make fix` for autofix

**Types:** plan, assessment, bug-report, design, research, template

## Rules

- ✅ Use automation only; never create artifacts manually
- ✅ Naming: `YYYY-MM-DD_HHMM_{TYPE}_name.md`
- ✅ Location: `docs/artifacts/` subdirectories
- ✅ Always validate after changes: `make validate && make compliance`
- ❌ Never modify files outside `docs/artifacts/` for validation tasks

## Direct Qwen CLI

If wrapper fails:
```bash
qwen --approval-mode yolo --include-directories /workspaces/upstageailab-ocr-recsys-competition-ocr-2 --prompt "[task with AgentQMS context]"
```

## Reference

- Config: `.qwen/settings.json`
- Scripts: `.qwen/run.sh`
- Automation: `AgentQMS/interface/Makefile`
- SST: `AgentQMS/knowledge/agent/system.md`
