{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "OCR ETL Configuration",
    "type": "object",
    "properties": {
        "input_dir": {
            "type": "string",
            "description": "Path to the root directory of the input dataset containing images and labels."
        },
        "output_dir": {
            "type": "string",
            "description": "Path where the generated LMDB dataset will be stored."
        },
        "num_workers": {
            "type": "integer",
            "description": "Number of parallel worker processes to use.",
            "default": 4,
            "minimum": 1
        },
        "batch_size": {
            "type": "integer",
            "description": "Number of samples to process before writing to LMDB transaction.",
            "default": 1000,
            "minimum": 1
        },
        "limit": {
            "type": [
                "integer",
                "null"
            ],
            "description": "Limit the number of files to process (useful for testing).",
            "minimum": 1
        }
    },
    "required": [
        "input_dir",
        "output_dir"
    ],
    "additionalProperties": false
}
